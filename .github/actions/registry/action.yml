# .github/workflows/docker-build.yml
name: Docker publish 

inputs:
  container-access-token:
    description: 'El token para loguearse'
    required: true

runs:
  using: "composite"
  steps:
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ inputs.container-access-token }} 

      - name: Build Docker image
        shell: bash
        run: |
          docker build -t ghcr.io/${{ github.repository_owner }}/${{ github.event.repository.name }}:${GITHUB_SHA::7} .

      - name: Push Docker image
        shell: bash
        run: |
          docker push ghcr.io/${{ github.repository_owner }}/${{ github.event.repository.name }}:${GITHUB_SHA::7}